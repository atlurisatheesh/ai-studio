# --- Core API (always required) ---
fastapi>=0.110
uvicorn[standard]>=0.25
python-dotenv>=1.0
aiosqlite>=0.20
pydantic>=2.6
email-validator>=2.2
pyjwt>=2.10
bcrypt>=4.1
httpx>=0.27
python-multipart>=0.0.9

# --- Local AI engines ---
# Speech-to-text (CPU or GPU). Whisper large-v3 = commercial-grade accuracy.
faster-whisper>=1.0

# TTS fallback that runs real-time on any CPU (MIT license)
piper-tts>=1.2

# TTS + zero-shot voice cloning, near-ElevenLabs quality (MIT license).
# Heavy (pulls torch); install on the GPU host:
#   pip install chatterbox-tts
# chatterbox-tts

# --- Dev ---
pytest>=8.0
pytest-asyncio>=0.23
